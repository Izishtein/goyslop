# Состояние проекта — SW 2.5 Character Sheet

Дата: 2026-09-06

---

## Текущий статус

**Стадия: MVP-чеклист (`features.md`), визуальный дизайн (`docs/design.md`) и почти вся спецификация листа закрыты (2026-08-12).** Сверх MVP сделаны опыт и уровни, заклинания, прирост, расовые способности, репутация, базовая информация, заметки, инвентарь расходников, секция Techniques/Spellsongs/Finales, Abyss Enhancement, каталоги фитов и расходников, пакеты проверок, экран-справочник и каталоги трёх школ магии из дополнений (2026-09-06). **Спецификация листа закрыта полностью.**

Приложение реально работает и оформлено: создание персонажа (раса → предыстория → характеристики), полный лист (характеристики с журналом прироста, классы с покупкой уровней за опыт, репутация и ранг, боевые параметры, снаряжение с инвентарём, боеприпасами и Abyss Enhancement, заклинания с каталогом на 398 штук, техники и песни Enhancer/Bard с каталогом на 45 штук, SCA с каталогом на 121 фит, Evocations Алхимика с каталогом на 26, снаряжение и маунты Core III, статус-эффекты с автоматикой, портрет, заметки и связи), сохранение в localStorage, экспорт/импорт JSON, EN/RU, печатный вид. Современный clean UI со светлой и тёмной темой, адаптивный. 309/309 тестов, `tsc -b`/`oxlint`/`vite build` зелёные, весь функционал прогонялся в реальном браузере (Playwright), не только в jsdom. Подробная хронология сессии — сразу ниже, самая свежая запись внизу списка.

**Главное, что осталось:** вопрос приватности GitHub Pages перед деплоем (нерешён) и решение по one-click PDF — оба требуют ответа пользователя, а не работы. Работы по спецификации листа не осталось — см. «Следующий шаг» в конце файла.

### Карта проекта (чтобы не читать всю хронологию)

Запуск: `npm install && npm run dev`. Проверки: `npm test` (Vitest), `npx tsc -b`, `npx oxlint`, `npm run build`.

| Где | Что |
|---|---|
| `src/types/character.ts` | Zod-схема персонажа — **единственный источник правды о форме данных**. Новые поля добавлять с `.default(...)`, иначе сломаются сохранённые персонажи |
| `src/lib/formulas/` | Чистые функции всех расчётов SW2.5 + юнит-тесты рядом (HP/MP, модификаторы, оружие, статус-эффекты, стоимость уровней, ранг по репутации, требования по силе и слоты SCA). Формулы не дублировать в компонентах |
| `src/lib/avatar.ts` | Уменьшение портрета перед записью в localStorage (320 px, JPEG) |
| `src/data/races.ts`, `classes.ts`, `spells/`, `arts.ts`, `combat-feats.ts`, `abyss.ts`, `consumables.ts`, `equipment.ts`, `mounts.ts` | Каталоги 18 рас (кости A–F, background-таблицы, расовые способности), 24 классов, 398 заклинаний (Core I + Core II + Fairy Magic + Nature/Summoning/Abyssal из дополнений), 45 техник/песен/финалов, 121 фита SCA (Core I–III), 26 Evocations Алхимика, снаряжения Core III (63 оружия, 16 брони, 9 щитов, 45 предметов), 17 маунтов с таблицами по уровням и 27 предметов снаряжения маунта, усилений и 36 проклятий Бездны, подсказок для расходников |
| `src/state/characters.ts` | Jotai-атомы + localStorage. Чтение проходит через `CharacterSchema.safeParse` — там же живёт миграция схемы |
| `src/features/character-creation/` | Экран создания персонажа |
| `src/features/character-sheet/` | Лист: `CharacterSheetView` (карточка с портретом, полом/возрастом, расовыми способностями) + секции Ability (с журналом прироста) / Classes (опыт и репутация) / CombatStats / Equipment / Spells / Arts (Techniques, Spellsongs, Finales — только у Enhancer и Bard) / CombatFeats / StatusEffects / Notes. Общий CSS-модуль на все секции |
| `src/features/character-io/` | Экспорт/импорт JSON |
| `src/features/reference/` | Экран-справочник: десять вкладок (расы, классы, предыстории, заклинания, фиты, техники, Evocations, снаряжение, маунты, предметы) плюс `RaceFactsCard` — та же карточка встроена в форму создания |
| `src/lib/backgrounds.ts` | Таблицы предысторий: список по расе плюс кросс-расовые индексы «кто может быть X» |
| `src/App.tsx`, `src/App.test.tsx` | Шапка, ростер персонажей (переключение, экспорт, удаление с подтверждением) и выбор «лист или форма создания» |
| `src/index.css` | Токены дизайна и обе темы |
| `src/print.css` | Глобальные print-правила. То, что зависит от классов CSS-модулей, — в `@media print` внутри самих модулей |
| `docs/sheet-content/` | Весь книжный ресёрч (расы, классы, формулы, заклинания) — источник для данных |
| `docs/design.md`, `features.md`, `tech-stack.md`, `legal.md` | Принятые продуктовые решения |

**Грабли, на которые уже наступали** (не повторять):
- **`.min(1)` на имени строки, которую UI создаёт пустой, = потеря персонажа.** Чтение из localStorage отбрасывает персонажа целиком, если хоть одна вложенная строка не прошла валидацию. Пустая строка — законное промежуточное состояние листа с инлайн-редактированием.
- Поля форм связывать **явными `htmlFor`/`id`**, не вложением `<label><select>…</label>` — иначе доступное имя поля склеивается с текстом выбранной опции и меняется при выборе.
- Любое изменение UI **проверять в реальном браузере**, а не только jsdom-тестами: так были найдены переполнение таблицы при печати и на экране, поломанная a11y, протечка тёмной темы, нерабочее определение локали.
- После правок вёрстки **перепроверять печать** — она завязана на семантику разметки.
- **`<textarea>` на печати не прокручивается** — то, что не влезло в высоту поля, на бумагу не попадёт. Поля с текстом должны расти под содержимое (плюс рамки к `scrollHeight` при `box-sizing: border-box`).
- **Картинки от пользователя ужимать перед localStorage** — квота на весь origin несколько мегабайт, одна фотография выносит весь ростер.
- **`transition` на цвете ломает печать:** правило печати может матчиться верно, а на бумагу попасть экранный цвет, если свойство анимировано (поймано на полосках HP/MP). В `@media print` анимации гасить.
- Проверяя печать через Playwright, **давать странице осесть после `emulateMedia({ media: 'print' })`** — иначе замеряется середина перехода, а не итоговый вид.
- **Скрипт для Playwright класть в корень репозитория, а не во внешнюю временную папку** — Node ищет `node_modules` вверх от файла скрипта, из чужой директории `import 'playwright'` не резолвится. Временный файл потом удалять.
- **Правила печати листа враждебны длинному каталогу.** `print.css` ставит всем таблицам `break-inside: avoid` и резервирует 18% под первую колонку — верно для секции на десяток строк, но таблица на 257 строк от этого либо вылезает за страницу, либо целиком прыгает на следующую. Для справочника оба правила переопределены, плюс `thead { display: table-header-group }`, чтобы шапка повторялась.
- **Ключ React по имени — ловушка там, где имена повторяются.** У расы одна и та же способность встречается на разных уровнях (`[Sixth Sense]` на 0, 6 и 11), и `key={ability.name}` дал дубли. jsdom это не роняет — React только пишет в консоль, а браузерная проверка поймала сразу.
- **Кнопка-переключатель не должна переименовываться.** Кнопка «Справочник» с `aria-pressed` сначала меняла подпись на «Вернуться к листу» — и совпадала по имени с кнопкой внутри самого справочника, а заодно сообщала состояние дважды и противоречиво. Состояние несёт `aria-pressed`, подпись стоит на месте.
- **`<input>` не переносит текст, а на печати таблицы фиксированной ширины** — длинное значение просто обрезается краем ячейки, и написанного игроком на бумаге нет. Свободный текст в таблицах идёт через `PrintableField`: поле для экрана плюс печатный двойник-`<span>`, который переносится.
- **Placeholder печатается как данные.** Пустое поле уходило на бумагу с подсказкой «rope, lantern, backpack…», будто это ввод игрока. `::placeholder` в `@media print` прозрачный.
- **`<input list=…>` рисует стрелку выбора** (`::-webkit-calendar-picker-indicator`), и на бумаге она садится посреди данных. Скрыта в print.css.
- **Числа-трекеры зажимать при записи И при показе.** Полоска HP зажималась до 100%, а число рядом показывало 30/23; сохранённое значение к тому же переживает максимум, который его оправдывал (упал VIT — вчерашние 23 стали невозможны).
- **Не всякий `atomWithStorage` защищён тем, что вы обернули один.** `localStorage.setItem` бросает при переполнении квоты и в заблокированном браузере; ловить надо в каждом хранилище (персонажи И активный id), иначе исключение прилетает в середину React-обновления. Тест на это написан именно так — и сразу поймал незащищённое второе хранилище.
- **Захардкоженный диапазон в UI переживает расширение данных молча.** Пикер заклинаний строил группы кругов по литералу `[1..10]` — с приходом школ дополнений (до 15 уровней) треть каталога просто не отрисовалась бы, без ошибки и без предупреждения. Диапазоны выводить из данных.
- **В книжных PDF уровень заклинания — не текст, а картинка.** Полоса «3rd Level Nature Spells» и кружок-значок у названия не попадают в текстовый слой: `pdftotext` отдаёт все названия, стоимости и эффекты, но круг придётся снимать с отрисованной страницы (`scripts/render-pdf-pages.mjs`).
- **`npm install --no-save A` выкидывает ранее так же поставленный B.** npm вычищает всё, чего нет в манифесте, — установка `pdfjs-dist` снесла `playwright`. Ставить всё нужное одной командой.
- Пара «заголовок + пустой блок» на печати убирается обёрткой `.subsection` (`display: contents` на экране) с атрибутом `data-print-empty` — считать пустоту в React, а не угадывать её селекторами.

---

## Хронология (от старой к новой)

Стек, MVP и все продуктовые решения приняты (2026-07-07, дополнены 2026-08-04). Контент листа персонажа задокументирован в `docs/sheet-content/` (00–12 + новые находки) для Core Rulebook I–III полностью по расам/классам/формулам/заклинаниям базового уровня. **Все 8 дополнений сообщества разобраны** (7 полностью + 1 частично из-за эмбарго переводчиков) — книжный ресёрч по классам завершён, **все 24 класса SW2.5 подтверждены по первоисточникам**. Полная структура и навигация: `docs/sheet-content/00-index.md`.

**2026-08-04 (аудит):** пройден пред-стартовый аудит доков, дозакрыты 2 продуктовых вопроса — HP/MP трекер + статус-эффекты (полноценно, с автоматикой, в MVP) и стратегия тестирования (Vitest + Testing Library) — см. `docs/features.md`, `docs/tech-stack.md`. Аудитом также найдены 2 риска: (1) **GitHub Pages без GitHub Pro/Team** сделает сайт публичным, что противоречит приватности, на которой держится `legal.md` — пока не решено, обсудить в следующей сессии; (2) **fan-translation PDF в `files/` (10 книг) оказались уже закоммичены и запушены** в `origin/master` (выяснилось при пуше — коммит был сделан ранее, вне этой сессии). Решение принято: оставить как есть (приватный репозиторий, история не переписывается) — зафиксировано в `docs/legal.md`.

**2026-08-04 (старт разработки):** поднят каркас проекта — Vite + React 19 + TypeScript, зависимости из `tech-stack.md` установлены (Jotai, Zod, i18next/react-i18next, Radix UI пакет `radix-ui`, Vitest + Testing Library). Собраны:
- `src/types/character.ts` — Zod-схема персонажа (`schemaVersion`, характеристики, классы/уровни, HP/MP current, статус-эффекты) с выведенными TS-типами
- `src/lib/formulas/` — чистые функции формул (модификатор характеристики, HP/MP max, Fortitude/Willpower/Accuracy/Evasion/Extra Damage/Magic Power, тик статус-эффектов по раундам) + Vitest-тесты на каждую (27 тестов, все зелёные)
- `src/state/characters.ts` — Jotai-атомы поверх `atomWithStorage` (localStorage) для списка персонажей и активного персонажа
- `src/i18n/` — i18next с ресурсами en/ru, переключатель языка в UI
- Заглушка `App.tsx` вместо шаблона Vite; `npm run build` и `npm test` (алиас на `vitest run`) проходят

**Решения по ходу сборки** (записаны на момент старта; часть с тех пор ушла дальше — актуальное состояние см. в записях ниже и в разделе «Открытые вопросы»):
- Статус-эффекты: список модифицируемых полей — фиксированный enum (6 характеристик + accuracy/evasion/fortitude/willpower/defense/actionCheck) плюс `custom` с свободным `customLabel` как запасной вариант. Раунды — независимый счётчик на каждый эффект (не общий на бой). Пресеты частых эффектов — не реализованы, только ручной ввод. Это решение так и осталось в силе.
- Radix UI подключён единым пакетом `radix-ui` (актуальная рекомендация Radix вместо россыпи `@radix-ui/react-*`). **Актуально:** ни один примитив так и не понадобился — весь UI на нативных контролах (см. `docs/design.md`).
- ~~Каталоги рас/классов в схему пока не заведены~~ — сделано в тот же день, см. запись ниже.

**2026-08-04 (каталоги рас/классов):** добавлены `src/data/races.ts` (18 рас: id, кости A–F для 12 рас Core I–III, restricted classes; Outlaw-расы и Abyssborn/Newman помечены флагами `usesVagrantSystem`/`missingDiceData` вместо костей) и `src/data/classes.ts` (24 класса: type warrior/wizard/other, rank major/minor, magicSchool для волшебных). Добавлена `src/lib/formulas/xp-cost.ts` (стоимость уровня класса по таблице Core II/III, не по неточной фразе "1000 XP за уровень" из старых заметок — расхождение с построчной таблицей, таблица как более подробный источник имеет приоритет). Тесты: 36/36 зелёных, `tsc -b` и `vite build` проходят.

**Осознанно не перенесено в данные (следующие шаги, не блокируют):** построчные описания расовых способностей (текстовые эффекты вроде [Darkvision]) — пока не нужны для расчётов, только для отображения на листе.

**2026-08-04 (background-таблицы + первый UI-экран):** доверстаны background-таблицы во все 12 рас с костями (`src/data/races.ts`) — по 2 таблицы (primary/additional) на расу, каждая строка: диапазон 2d6, имя, стартовые классы (с семантикой "and"/"or" для составных вариантов типа "Sorcerer or Conjurer"), Skill/Body/Mind, XP. Собран первый рабочий экран:
- `src/features/character-creation/CharacterCreationForm.tsx` — выбор расы → фона (с авто-подстановкой Base характеристик) → стартового класса (если у фона выбор "or") → ручной ввод Correction/Growth/Item Bonus → живой пересчёт Total/Modifier и превью HP/MP max → сохранение персонажа (валидация через `CharacterSchema.parse`) в Jotai/localStorage
- `src/features/character-sheet/CharacterSheetView.tsx` — сводка персонажа: расы/фон/классы, HP/MP трекер (current редактируется, кнопка сброса к max), Fortitude/Willpower, таблица характеристик, заглушка статус-эффектов
- `App.tsx` — список персонажей / переключение / создание нового / удаление
- Добавлены `src/lib/formulas/character-levels.ts` (adventurerLevel, wizardLevelSum) и расширены `abilities.ts`/`ability-base.ts` вспомогательными функциями
- i18n-ключи для всего нового UI (en/ru)
- Component-тесты (Testing Library) на золотой путь создания персонажа: пересчёт при вводе характеристик, выбор класса при "or"-фоне, сохранение в store
- Итог: 55/55 тестов зелёные, `tsc -b` и `vite build` без ошибок. **Не проверено в реальном браузере** (только jsdom-тесты) — стоит открыть `npm run dev` и пройти golden path глазами перед тем, как считать экран готовым к показу.

**Известные упрощения экрана создания (не блокируют, но заметны):** ~~нет валидации диапазона Correction~~ и ~~удаление персонажа без подтверждения~~ — закрыто 2026-08-12 (см. запись в конце хронологии); ~~визуальный дизайн сырой~~ — закрыт вместе с `design.md`. Осталось: нет отображения расовых способностей/restricted classes.

**2026-08-04 (проверка в реальном браузере):** golden path прогнан через Playwright/Chromium (временно, не добавлено в devDependencies — `npm install --no-save`) поверх запущенного `vite dev`: создание персонажа (раса → фон → коррекция → сабмит) → лист персонажа с верными HP/MP/Fortitude/Willpower/характеристиками, без ошибок в консоли. По ходу проверки найдены и исправлены 2 реальных бага (не только тестовые придирки):
- **A11y:** поля формы были `<label>Текст<select>…</select></label>` (неявная вложенность) — доступное имя поля вычислялось браузером как "текст label + текст выбранной опции" и менялось при каждом выборе, что ломает скринридеры. Исправлено на явные `htmlFor`/`id` с раздельными label/control (`CharacterCreationForm.tsx`).
- **Вёрстка:** в `src/index.css` оставались правила из шаблона Vite (h1 56px, `#root` шириной 1126px с `text-align:center`) — из-за них заголовок наезжал на список персонажей. Шаблонные стили вычищены, оставлены только CSS-переменные темы и базовые h1/h2/code.

Тесты (55/55), `tsc -b`, `vite build` — все зелёные после исправлений.

**2026-08-04 (лист персонажа — боевые параметры/снаряжение/SCA):** по фидбеку "лист выглядит неполным" добавлены 4 раздела на `CharacterSheetView`:
- Полная разбивка характеристик — Base/Correction/Growth/Item Bonus теперь редактируются прямо на листе (не только при создании), Total/Modifier пересчитываются live
- Боевые параметры — Accuracy/Evasion/Extra Damage на каждый Warrior-type класс персонажа (Marksman не даёт свой уровень в Evasion, как в правилах), Magic Power на каждый Wizard-type класс
- Снаряжение — оружие (Name/Stance/Min STR/Accuracy bonus/Total Accuracy/Power/Critical Value/Extra Damage bonus/Total Extra Damage/Rank), броня, щит, до 10 аксессуаров, валюта (Cash/Savings/Debt, старт 1200 G). Total Defense/Total Evasion считаются от экипировки + базового Evasion (по старшему Warrior-классу)
- SCA / Combat Feats — список с полем "название" (свободный ввод) + категория (Passive/Declaration/Major Action/Auto-Acquired). **Осознанно без каталога** — полный список ~70 фитов Core I+II не перенесён в данные (объёмная механическая справка, значительная выжимка книги целиком), игрок вводит вручную то, что выбрал по книге

Новые схемы (`equipment`, `currency`, `combatFeats`) и формулы (`weapon-stats.ts`) в `types/character.ts`/`lib/formulas/`. Компонент разбит на `AbilitySection`/`CombatStatsSection`/`EquipmentSection`/`CombatFeatsSection`. Тесты 62/62, `tsc -b`/`vite build` зелёные, golden path прогнан в реальном браузере (Playwright, временно) — числа сходятся с формулами, ошибок в консоли нет, вёрстка не ломается.

**Не сделано (осознанно, вне 4 выбранных пунктов):** каталог самих SCA-фитов (только свободный ввод), Reputation/Adventurer Rank, инвентарь расходников, Abyss Enhancement, отображение расовых способностей — всё это следующие возможные шаги.

**2026-08-04 (найденный и исправленный краш + экспорт/импорт JSON):**

*Баг от пользователя:* реальный краш в браузере — `character.equipment is undefined`. Причина: персонаж, сохранённый в `localStorage` до добавления `equipment`/`currency`/`combatFeats`, грузился без валидации и ронял `EquipmentSection`. Исправлено по существу (не заплаткой на симптом):
- `equipment`/`currency`/`combatFeats` в `CharacterSchema` теперь `.default(...)` — Zod сам подставляет пропущенные поля при парсинге
- `state/characters.ts`: `charactersAtom` читает `localStorage` через обёртку, которая прогоняет каждого персонажа через `CharacterSchema.safeParse` при каждом чтении — общее решение открытого вопроса "миграция JSON при старой schemaVersion" (пока — silent backfill через дефолты, не versioned-миграция с явными шагами; если понадобится ломающая смена формата — тогда нужен будет полноценный migration-раннер по `schemaVersion`)
- Персонажи, которые вообще не парсятся — тихо отбрасываются с `console.warn`, не роняя весь список
- Воспроизвёл ровно тот баг-репорт в реальном браузере (seed `localStorage` через Playwright `addInitScript`) — подтвердил и краш до фикса, и его отсутствие после

*Экспорт/импорт JSON (последний крупный пункт MVP-чеклиста, кроме статус-эффектов и печати):*
- `features/character-io/characterIo.ts` — чистые функции `characterToJson`/`characterFileName`/`parseImportedCharacter` (используют ту же `CharacterSchema.safeParse`, что и миграция при загрузке — экспорт из старой версии приложения тоже импортируется корректно)
- `downloadCharacter.ts` — скачивание через Blob/`URL.createObjectURL`; `ImportCharacterButton.tsx` — файловый инпут + понятная ошибка (`role="alert"`) при невалидном JSON, без падения приложения
- Кнопка "Download JSON" у каждого персонажа в списке, "Import JSON" в шапке нав-панели; импорт одного и того же `id` перезаписывает существующего персонажа (не дублирует)
- Round-trip проверен и в vitest, и в реальном браузере (Playwright: скачал → удалил персонажа → импортировал обратно → лист восстановился)

Тесты 72/72, `tsc -b`/`vite build` зелёные.

**2026-08-04 (UI статус-эффектов):** данные/формулы (`StatusEffectSchema`, `tickStatusEffects`, `sumModifiersForField`) существовали с самого начала разработки, но не были подключены к листу — теперь подключены:
- `StatusEffectsSection.tsx` — список активных эффектов (имя, источник, длительность, модификаторы), кнопка "Next round" (тикает раунды, авто-снимает истёкшие через `tickStatusEffects`), форма добавления (имя/источник/длительность — rounds/permanent/untilRemoved, произвольное число модификаторов field+value, `custom` с свободным лейблом)
- Модификаторы статус-эффектов подмешаны в уже существующие расчётные поля через `sumModifiersForField`, а не выведены отдельной колонкой: Fortitude/Willpower (`CharacterSheetView`), Accuracy/Evasion по каждому Warrior-классу (`CombatStatsSection`), Total Defense/Total Evasion (`EquipmentSection`), модификатор каждой характеристики (`AbilitySection`) — везде видно уже посчитанное готовое число
- По ходу той же правки исправил ту же a11y-ошибку с неявным `<label>` в форме добавления эффекта (см. запись про `CharacterCreationForm` выше) — сразу через явные `htmlFor`/`id`, не наступая на грабли повторно
- Проверено в реальном браузере: наложение эффекта на Fortitude меняет отображаемое число, "Next round" уменьшает счётчик раундов и корректно снимает эффект по истечении, значение возвращается к базовому

Тесты 73/73, `tsc -b`/`vite build` зелёные.

**2026-08-12 (печатный вид — последний пункт MVP-чеклиста):**

*Отклонение от записанного решения — требует подтверждения.* В `features.md`/`tech-stack.md` было записано «кнопка "Скачать PDF" (react-to-print / @react-pdf/renderer), **не только CSS print**». При реализации выяснилось: **react-to-print не генерирует PDF** — он открывает системный диалог печати для изолированного компонента, то есть даёт ровно тот же артефакт, что `window.print()` + print-стили (в диалоге есть «Сохранить как PDF»). Настоящую генерацию PDF-файла даёт только `@react-pdf/renderer`, но он требует заново собрать всю вёрстку листа на своих примитивах (Document/Page/View/Text) — это полный дубль разметки, который неизбежно разъедется с реальным листом.

Сделано: кнопка «Печать / Сохранить в PDF» (`window.print()`) + отдельный `src/print.css`. Зависимостей не добавлено. **Если нужен именно one-click download .pdf без диалога — это отдельная задача на `@react-pdf/renderer` с дублирующей вёрсткой, решать отдельно.**

`src/print.css` (всё внутри `@media print`, экранный вид не затронут):
- Скрыто по семантике, без спец-классов: `nav` (переключатель персонажей), переключатель языка, все `<button>` и `<form>` — на листе персонажа это исключительно органы управления, данные живут в инпутах
- Инпуты печатаются как значения на тонкой линейке (без рамок/спиннеров), а не как формы
- Принудительно светлая палитра (чёрным по белому) независимо от темы системы
- `table-layout: fixed` + `width: 100%` — **это ключевой фикс**, см. ниже
- Первая колонка 18% (в каждой таблице листа это «название»), заголовки 7.5pt без переноса посреди слова
- Колонка с кнопками Remove убрана целиком (`th:empty`, `td:has(> button)`) — на бумаге это была пустая колонка, в таблице SCA съедала четверть ширины

*Найдено и исправлено проверкой в реальном браузере (не поймалось бы никакими jsdom-тестами):* при первой версии стилей таблица оружия имела ширину **1435px против 703px печатной ширины A4** — правые колонки (Power, Critical Value, Rank и т.д.) просто обрезались бы при печати. Причина: 11 колонок с инпутами фиксированной ширины. Лечится `table-layout: fixed`. Отдельно ловилась обрезка названия оружия внутри инпута (`scrollWidth` 76 против `clientWidth` 50) — текст инпута не переносится, лечится расширением первой колонки.

*Как проверялось:* Playwright с `emulateMedia({ media: 'print' })` и viewport, равным реальной печатной ширине A4 (703px = 210mm − поля 12mm при 96dpi), плюс генерация настоящего PDF через `page.pdf()`. Проверено программно: nav/кнопки/форма скрыты, данные инпутов сохранены, `body.scrollWidth` не превышает ширину страницы, цвета вычисляются как `rgb(0,0,0)`.

Тесты 73/73, `tsc -b`/`vite build` зелёные.

**MVP-чеклист (`features.md`) закрыт полностью.** Все пункты: лист со всеми секциями, автокалькуляция, HP/MP-трекер, статус-эффекты, автосохранение, экспорт/импорт JSON, EN/RU, печатный вид, список персонажей.

**2026-08-12 (визуальный дизайн — `docs/design.md` закрыт):** решения приняты пользователем (все зафиксированы в `docs/design.md`): современный clean UI, один длинный скролл, десктоп-first но responsive, в RU-локали «Русский (English)».

Сделано:
- Токены дизайна в `src/index.css` — поверхности/текст/акцент/семантика, шкала отступов, радиусы, тени; светлая и тёмная темы по `prefers-color-scheme`
- `App.module.css` (бывший `App.css`, переведён в CSS Module) — шапка, ростер персонажей карточкой с активным состоянием
- Лист: карточка личности с бейджами классов, блок витальных показателей с **прогресс-барами HP/MP** (цвет HP по опасности: >50% зелёный, 25–50% янтарный, <25% красный), характеристики **карточками 3×2** вместо таблицы, секции-карточки, оформленные таблицы/инпуты/кнопки, статус-эффекты списком с чипами модификаторов
- Форма создания перестроена в том же языке
- RU-локаль: игровые термины как «Точность (Accuracy)», «Стойкость (Fortitude)» и т.д.

*Найдено и исправлено проверкой в браузере (ничего из этого не ловится jsdom-тестами):*
1. **Таблица оружия переполняла страницу на 181px** на десктопе 1100px — колонки Rank и Remove уходили в горизонтальный скролл, название оружия обрезалось. Причина оказалась не в инпутах, а в `white-space: nowrap` на заголовках: «EXTRA DAMAGE BONUS» в одну строку распирал колонку. Лечится переносом заголовков + минимальной шириной колонки имени.
2. **Русская локаль не определялась автоматически:** браузер сообщает `ru-RU`, точного ресурса нет → фолбэк на английский. Добавлен `load: 'languageOnly'`.
3. **Следом всплыл связанный дефект:** интерфейс русский, а переключатель показывал «EN», т.к. `i18n.language` = `ru-RU` не совпадает ни с одной опцией. Переведён на `i18n.resolvedLanguage`.

Печать перепроверена после редизайна (риск был отмечен заранее): `print.css` разделён — глобальное осталось в нём, а то, что зависит от хешированных классов CSS-модулей (карточки → плоские блоки, сетка характеристик 3×2), переехало в `@media print` внутрь самих модулей. Тёмная тема в печать не протекает, всё помещается в A4.

Проверено в Chromium: десктоп светлая/тёмная, мобильная 390px (переполнения нет), печать на реальной ширине A4 + генерация PDF, определение локали `ru-RU`/`en-US`/`ru` и сохранение выбора после перезагрузки. Тесты 73/73, `tsc -b`/`oxlint`/`vite build` чистые.

**Тесты пришлось обновить** под новую разметку (характеристики из таблицы стали карточками): запросы переведены с `getByRole('row'/'cell')` на `getByLabelText('STR Total')` и т.п. Для этого добавлены осмысленные `aria-label` на значения — то есть тесты стали опираться на доступные имена, а не на структуру DOM.

**2026-08-12 (сверка с планом + полировка):** прогнан аудит «всё ли идёт по плану». Код оказался ровно в заявленном состоянии (тесты/`tsc`/`oxlint`/`vite build` зелёные, рабочее дерево чистое), но нашлось расхождение в доках: `features.md` держал MVP-чеклист целиком неотмеченным, хранил устаревшее решение про `react-to-print` и числил закрытые вопросы (миграция схемы, детали статус-эффектов) как открытые; в самом `state.md` записи про печать и дизайн были помечены 2026-08-04, хотя коммиты датированы 2026-08-12. Всё приведено в соответствие.

Затем закрыты три пункта из «В) Полировка»:
- **Валидация Correction против расового кубика** — `correctionRange`/`isCorrectionInRange` в `lib/formulas/ability-base.ts`. В форме создания значение вне диапазона кубика подсвечивается (`aria-invalid` + янтарная рамка) и объясняется строкой «DEX — 2d6 (2–12)». **Предупреждение, а не блокировка**: сабмит остаётся доступен (house rules), а ноль считается «ещё не введено», чтобы свежая форма не встречала игрока шестью красными полями
- **Подтверждение удаления персонажа** — вместо мгновенного удаления первый клик по «Удалить» разоружает кнопку в пару «Удалить N?» / «Отмена» прямо в строке ростера. Инлайн, а не модалка: ошибка происходит именно там, и лист не обрастает диалоговой машинерией (Radix по-прежнему не нужен)
- **Пустые блоки снаряжения на печати** — сабблоки Weapons/Armor/Shield/Accessories обёрнуты в `.subsection` (`display: contents` на экране) с атрибутом `data-print-empty`, который в `@media print` убирает пару «заголовок + пустота» целиком. Секции с явной строкой «нет ничего» (SCA, статус-эффекты) заголовок сохраняют — там есть куда вписать от руки

*Найдено проверкой в браузере:* полоски HP/MP печатались цветными, хотя `print.css` задаёт серую заливку `#777 !important`. Причина не в приоритетах CSS — правило матчилось верно, — а в `transition: background-color 0.2s` на `.fill`: страница, растеризованная в момент переключения на print-медиа, успевала поймать экранный зелёный. Лечится `transition: none` в печатном блоке.

Проверки: 86/86 тестов (добавлены `src/App.test.tsx` на подтверждение удаления и тесты диапазона коррекции), `tsc -b`/`oxlint`/`vite build` зелёные (после проверки предысторий — 90/90). В реальном браузере (Playwright, `--no-save`) прогнаны 28 проверок: golden path, обе ветки подтверждения удаления, печать на ширине A4 без переполнения, скрытие/возврат пустых блоков, генерация PDF, RU-локаль — все зелёные, ошибок в консоли нет.

**2026-08-12 (проверка предысторий):** по вопросу «правильно ли работают предыстории» прогнан аудит данных и потока создания.

*Данные чистые.* Разовый скрипт проверил все 12 таблиц: диапазоны 2d6 покрывают 2–12 без дыр и пересечений в каждой таблице, все classId существуют в каталоге классов, ни одна предыстория не выдаёт класс из restrictedClasses расы, дублирующихся подписей в выпадающем списке нет. Человеческая таблица сверена построчно с `docs/sheet-content/01-races.md` — сходится. Единственные «особые» строки — те, что такими и заданы в книге: Normal (7) и Adventurer (※) у человека и Traveler (7) у grassrunner не дают стартового класса, а Adventurer вдобавок бросает свои Skill/Body/Mind вместо фиксированных.

*Три бага в UI, все исправлены:*
1. **Строка, бросающая свои Skill/Body/Mind, была неиграбельна.** Adventurer выбирался, но вводить бросок было негде — все Base оставались нулями, и персонаж создавался с DEX/AGI/STR/VIT/INT/SPR = 0 + коррекция. Добавлен блок ввода Skill/Body/Mind, который показывается только для строк с `stats: null`.
2. **HP при создании считался как если бы класс был всегда** (`hpMax(1, vitTotal)` захардкожено). Для предысторий без стартового класса Adventurer Level = 0, лист пересчитывал max по нулю — и сохранённый current HP оказывался выше max (16 при максимуме 13). Уровень для превью теперь выводится из числа стартовых классов.
3. **Обе таблицы предысторий были свалены в один плоский список.** Игрок бросает по одной таблице, а в списке рядом стояли «Artificer (2-4)» из основной и «Archer (2-4)» из дополнительной — не отличить. Список разбит на `<optgroup>` «Основная / Дополнительная (Core II)», а строка ※ теперь подписана «с разрешения GM», а не звёздочкой.

*Найдено, но не чинилось — это уже не про предыстории, а про отсутствующую механику (нужно решение пользователя):*
- **XP предыстории нигде не сохраняется.** В списке он показывается (2000/2500/3000), но в схеме персонажа поля опыта нет вообще, а `src/lib/formulas/xp-cost.ts` (полностью реализован и покрыт тестами) не используется ничем. Для Normal/Adventurer это критично: повышенные 3000 XP — это ровно та валюта, на которую покупается первый класс.
- **Классы нельзя добавить или прокачать после создания** — на листе они только бейджи. Персонаж с предысторией без стартового класса остаётся классовым нулём навсегда, а обычный персонаж не может взять второй уровень. Это самая большая дыра в текущем MVP; вместе с пунктом выше просится в отдельную фичу «опыт и уровни».

Проверки: 90/90 тестов (4 новых на предыстории), `tsc -b`/`oxlint`/`vite build` зелёные, в браузере прогнаны сгруппированный список, ввод брошенного Skill/Body/Mind, пересчёт Base/Total и RU-локаль.

**2026-08-12 (опыт и уровни — первая фича сверх MVP):** закрыт пункт Б0, найденный при проверке предысторий. До этого классы фиксировались при создании навсегда, а XP предыстории показывался в списке и терялся.

- **Схема:** `experience: { total, spent }` через `.default(() => ({ total: 0, spent: 0 }))` — старые сохранения добираются нулями и не ломаются (проверено сидом legacy-персонажа в localStorage через Playwright).
- **Создание:** `total` инициализируется XP предыстории, стартовые классы даются сверх него бесплатно — так в книге, поэтому у «Normal» без класса опыта и больше (3000 против 2000).
- **Секция «Классы и опыт»** (`ClassesSection.tsx`) между характеристиками и боевыми параметрами: `+1`/`-1` по уровню, добавление класса из каталога с фильтром по restricted classes расы, удаление. Стоимость уровня — из `xp-cost.ts` (таблица Core II/III), туда же добавлена константа `MAX_CLASS_LEVEL = 15`.
- **Ключевое решение — симметрия:** всякое автоматическое изменение `spent` есть точная обратная операция явной покупки (`+1` списывает полученный уровень, `-1` возвращает его же). **Удаление класса не возвращает ничего:** предыстория даёт стартовые классы бесплатно, и рефанд при удалении печатал бы опыт из воздуха. Оба числа остаются редактируемыми — это и есть штатный способ поправить расхождение.
- Перерасход не блокируется, а подсвечивается: остаток уходит в минус красным.

*Найдено при проверке печати:* `<select>` добавления класса на бумагу не убирался — `print.css` прячет `nav`/`button`/`form`, а select везде на листе является данными (хват и ранг оружия), поэтому скрыть их скопом нельзя. Строка помечена классом `.controlRow`, который прячется в печати — по образцу уже существовавшего `.langSwitch` в `App.module.css`.

*Тест, который сломался не по делу:* `CharacterSheetView.test.tsx` брал таблицу боевых параметров как `getAllByRole('table')[0]`, и новая секция сдвинула индекс. Переписан на поиск от заголовка секции — тест больше не зависит от порядка карточек на листе.

Проверки: 97/97 тестов (7 новых на секцию классов + проверка стартового XP), `tsc -b`/`oxlint`/`vite build` зелёные. В браузере — 20 проверок: загрузка legacy-персонажа без поля опыта, покупка класса, `+1`/`-1`, отсутствие рефанда при удалении, пересчёт Adventurer Level и MP max, красный перерасход, запись в localStorage, печать (таблица есть, пикер скрыт, A4 без переполнения), RU-локаль — все зелёные, ошибок в консоли нет.

**2026-08-12 (заклинания — §7 спецификации листа):** до этого раздел «Магия» не был реализован совсем: Magic Power по классам считался, а списка известных заклинаний на листе не было ни в каком виде, хотя половина каталога классов — волшебные.

- **Каталог Core I** `src/data/spells.ts`: 103 заклинания (Truespeech 23, Spiritualism 21, Divine 20 базовых + 16 специализированных по 8 богам, Magitech 23), перенесены из `sheet-content/09-spells-core1.md`. Тест сверяет счётчики по школам, уникальность id, диапазон кругов, по паре кругов 2/4 на каждого бога и совпадение названий школ с `classes.ts` — именно по ним секция подбирает заклинания персонажу.
- **Осознанно только индекс:** название, школа, круг, MP. Текст эффекта не переносится — в ресёрч-доках он только русский, а приложение двуязычное; вместо него у каждого известного заклинания своё поле заметок. `legal.md` встраивание данных книги разрешает (репозиторий приватный), так что это вопрос не права, а консистентности локалей — при желании эффекты добавляются отдельным проходом.
- **Секция** `SpellsSection.tsx`: школы берутся из волшебных классов персонажа, круги выше уровня класса не прячутся, а помечаются «above class level» (лист заполняют и на вырост), уже известное заклинание в списке гасится. Школы без каталога (Fairy Magic и прочие из дополнений) не ломают экран — предлагается ручной ввод, он же всегда доступен как запасной путь.

*Найден и починен баг потери данных, существовавший с самого добавления снаряжения:* каждая кнопка «Добавить …» создаёт пустую строку, которую игрок называет потом, но `name` в схемах строк был `z.string().min(1)`. При следующем чтении из localStorage строка не проходила валидацию, а `charactersStorage.getItem` отбрасывает **персонажа целиком** — то есть достаточно было добавить оружие, не вписать название и перезагрузить страницу, чтобы потерять весь лист. Воспроизведено в браузере (персонаж исчезал, в консоли `Dropping unreadable saved character`), вылечено снятием `.min(1)` со строк, которые лист создаёт пустыми (оружие, броня, щит, аксессуары, фиты, заклинания). У статус-эффектов `.min(1)` остался: их добавляет форма, которая пустое имя не пропускает. Регрессия закреплена тестом в `state/characters.test.ts`.

*Мелочь по ходу:* кнопки «Add» в секциях классов и заклинаний имели одинаковое доступное имя — переименованы в «Добавить класс» / «Добавить заклинание».

Проверки: 113/113 тестов (16 новых), `tsc -b`/`oxlint`/`vite build` зелёные. В браузере — 18 проверок: подстановка круга и MP из каталога, пометка недоступных кругов и её снятие после левелапа, гашение известного заклинания, ручной ввод для школы без каталога, сообщение для неволшебного персонажа, печать (таблица на бумаге, пикер скрыт, A4 без переполнения), RU-локаль.

**2026-08-12 (прирост, расовые способности, репутация, каталоги дополнений):**

- **Прирост (Growth).** В `AbilitySection` — кнопки «+1» по каждой характеристике и журнал прироста (`growthLog` в схеме, через `.default(...)`): что выросло и на каком уровне авантюриста. Удаление строки журнала забирает +1 обратно, поэтому журнал и колонка Growth не могут разойтись — та же симметрия, что у покупок опыта. **Броска по таблице роста нет и не будет без источника:** самой таблицы в ресёрч-доках не оказалось (в `sheet-content.md` она прямо числится открытым вопросом), поэтому лист записывает результат, который игрок бросил сам, а не изобретает механику.
- **Расовые способности.** Имена из `01-races.md` для всех 18 рас — в `races.ts` отдельной картой `RACIAL_ABILITIES` (чтобы не трогать 18 объектов) плюс `racialAbilitiesFor()`. На листе показаны в карточке персонажа; усиления Core II/III помечены `Lv6+`/`Lv11+` и приглушены, пока уровень до них не дорос. Тексты эффектов не переносились — то же решение, что по заклинаниям (в доках они только по-русски).
- **Репутация и ранг авантюриста.** `reputation` в схеме + `lib/formulas/adventurer-rank.ts` с таблицей Core II (12 ступеней) и продолжением «+★ за каждые 500» выше напечатанного. В секции «Классы и опыт» — поле репутации, выведенный ранг, бесплатный бюджет Renown Items и сколько очков до следующей ступени.
- **Каталоги школ дополнений.** +154 заклинания: Core II круги 7–10 для четырёх школ Core I (16/16/12), вторые пары специализированных божественных для 8 богов Core I и по 4 для 5 новых богов, плюс 60 Fairy Magic (6 стихийных типов). Каталог вырос со 103 до **257** и разложен по файлам: `data/spells/{types,core1,core2,fairy,index}.ts`. Данные Core II и Fairy извлечены разовым скриптом из markdown-таблиц доков (скрипт не сохранялся), счётчики сверены с итогами в самих доках и закреплены тестом.

*Чего в каталоге нет и почему (осознанно):* Basic Fairy Magic — док помечает названия как требующие верификации по PDF; Nature/Summoning/Abyssal/Arcane Magic — в доках только механика и счётчики, построчных списков нет; Techniques/Spellsongs/Finales — другая механика (требуемый уровень класса и ритм ♩/♡ вместо круга и MP), им нужна своя секция, а не таблица заклинаний. Для всех этих школ на листе работает ручной ввод. Заодно `mp` в каталоге стал необязательным: у Fairy Magic источник не печатает стоимость, и честнее показать «?», чем ноль.

*Мелочи, найденные проверкой:* выпадающий список кругов был захардкожен на 1–6 и не показывал бы Core II (7–10); подсказка про ранг наследовала uppercase от лейблов денежных полей и читалась как крик.

Проверки: 131/131 тестов (18 новых), `tsc -b`/`oxlint`/`vite build` зелёные. В браузере — 18 проверок: расовые способности эльфа с приглушёнными Lv6+/Lv11+, прирост и возврат при удалении строки, ранг Flamberge с бюджетом и разрывом до Sentinel, заклинание Fairy Magic с «? MP» и стихией, круг 10 Core II, печать на A4 без переполнения и со скрытыми кнопками прироста.

**2026-08-12 (базовая информация и заметки — §1 и §10 спецификации):** закрыты последние разделы листа, которых не было совсем.

- **Пол и возраст** — свободный текст в карточке персонажа. Возраст строкой намеренно: «около 200» у эльфа и «неизвестно» у рунфолка одинаково законны, число тут врало бы.
- **Портрет** — `src/lib/avatar.ts`: выбранный файл рисуется в canvas, ужимается до 320 px по длинной стороне и сохраняется как JPEG data URL. Причина не в красоте, а в квоте: персонажи лежат в localStorage, и фотография с телефона выбрала бы её целиком, уронив весь ростер. Проверено в браузере — исходник 2.8 МБ ложится в 2 КБ (320×240). Нечитаемый файл (текст под видом картинки) даёт сообщение в `role="alert"`, а не падение.
- **Заметки** (`NotesSection`) — история персонажа, личные цели, заметки мастера + таблица связей (имя / кто это / заметка). Пустые блоки не печатаются, как и остальные пустые сабблоки листа.
- **Автоматический рост textarea.** У textarea нет полосы прокрутки на бумаге: всё, что не влезло в её высоту, при печати просто пропало бы. Поле подгоняет высоту под содержимое, причём к `scrollHeight` прибавляются рамки — с `box-sizing: border-box` без этого последняя строка уходила на пару пикселей под нижний край (поймано браузерной проверкой, jsdom такое не видит).

*Заодно исправлены старые грабли:* переключатель языка был `<label><select>…</select></label>`, из-за чего его доступное имя склеивалось в «LanguageENRU» — ровно та ошибка, которая записана в «Граблях» с 2026-08-04 и была починена тогда только в форме создания. Теперь label и select — соседи.

Проверки: 138/138 тестов (7 новых), `tsc -b`/`oxlint`/`vite build` зелёные. В браузере — 18 проверок: сохранение пола/возраста, загрузка портрета с уменьшением и проверкой реального размера в localStorage, удаление портрета, ошибка на неизображении, рост поля заметок без обрезания, связи, печать (портрет есть, выбор файла скрыт, пустые заметки убраны, A4 без переполнения).

**2026-09-06 (инвентарь расходников — вторая половина §5 спецификации):** снаряжение перестало быть только оружием и бронёй.

- **Инвентарь** внутри секции «Снаряжение»: набор авантюриста одной строкой (книга и продаёт его одной строкой), таблица предметов «название / кол-во / вес / примечание» и отдельный счётчик боеприпасов. Боеприпасы не строка таблицы намеренно: стрелок меняет это число каждый бой, а тип остаётся прежним.
- **Вес — строка, а не число.** В SW2.5 нет правил переноски, так что писать туда чаще всего нечего, а принудительный `0` в каждой строке — просто шум на бумаге.
- **Подсказки расходников** (`src/data/consumables.ts`, 10 названий из `06-equipment.md`) — через `<datalist>`: подсказывает, но не ограничивает, как каталог заклинаний рядом со свободным вводом.
- **Журнал трат** — свободный текст «предмет :: цена» под деньгами, в `currency.spendingLog`. Растёт под содержимое тем же `autoGrow`, что и заметки: helper переехал из `NotesSection` в `character-sheet/autoGrow.ts`, чтобы не копировать вместе с ним и грабли про рамки при `box-sizing: border-box`.
- Пустой инвентарь целиком уходит с бумаги (`data-print-empty`), как остальные пустые сабблоки.

*Схема:* `equipment.inventory` и `currency.spendingLog` добавлены с `.default(...)`, так что персонажи, сохранённые до этого, читаются из localStorage без потерь (проверено тестом на старой форме данных). `EMPTY_INVENTORY` экспортируется из `types/character.ts` — им же пользуются фикстуры тестов.

Проверки: 147/147 тестов (9 новых), `tsc -b`/`oxlint`/`vite build` зелёные. В браузере — светлая и тёмная темы, RU и EN, добавление строки через UI, рост журнала трат без обрезания, печать A4 без горизонтального переполнения и без кнопок, исчезновение пустого инвентаря с бумаги.

*Известное ограничение (не новое):* длинное значение в узкой ячейке таблицы обрезается на печати — у `<input>` текст не переносится. Это одинаково верно для названий оружия, заклинаний и примечаний к предметам; чинится только заменой инпутов на print-only текст во всех таблицах сразу.

**2026-09-06 (Techniques, Spellsongs и Finales — §7 спецификации, три системы Core II):** у Enhancer и Bard появилась своя секция листа.

- **Почему не таблица заклинаний.** У этих трёх систем нет ни круга, ни MP: у техник — требуемый уровень Enhancer и длительность, у песен — ритм ♩/♡, Flourish и условие эффекта, у финалов — цена ритма и сопротивление. Отдельная секция `ArtsSection` с тремя таблицами.
- **Каталог** `src/data/arts.ts` — 20 техник + 17 песен + 8 финалов из `12-techniques-spellsongs.md`. **Текста эффектов в каталоге нет намеренно:** в доках он существует только по-русски, а наполовину английский каталог хуже, чем никакого. Механика (длительность, ритм, Flourish, сопротивление, тип) — есть, а что способность делает, игрок пишет в своё примечание. Ровно та же линия, что у каталога заклинаний.
- **Длительность — код, а не строка** (`10s`/`30s`/`3min`/`1h`/`instant`), чтобы «30 сек (3 р)» и «30 s (3 rounds)» печатались каждый на своём языке. Всё остальное (♩♡, `Neg`, `Psychic (Weak)`, названия питомцев) — игровые термины, остаются как в книге, как ранги B/A/S.
- **Булевы поля — `<select>`, а не чекбоксы.** На печати с контролов снимается оформление, и чекбокс превратился бы в пустой квадрат на бумаге; select печатает выбранное значение текстом. Проверено: на бумагу попали «△» и «30 s (3 rounds)».
- **Секции нет у тех, кому она не нужна.** Всё это принадлежит двум Minor-классам, и лист Fighter'а не должен таскать три пустые таблицы: секция появляется при Enhancer/Bard либо если строки уже записаны (например, класс сняли, а записи остались). Это осознанное отличие от «Заклинаний», которые показываются всем.
- **Трекер исполнения** — ритм ♩ и ♡ отдельными счётчиками плюс питомец: ритм копится исполнением и тратится на финалы, то есть это трекер как HP/MP, а не производное число. Bardic Power и стандарт Enhancer — производные (`derived-stats.ts`), оба = уровень класса + модификатор INT.

Проверки: 161/161 тестов (14 новых), `tsc -b`/`oxlint`/`vite build` зелёные. В браузере — добавление 8 записей из каталога через UI с проверкой сохранённых полей, светлая и тёмная темы, RU и EN, печать A4 без переполнения (таблица песен — самая широкая на листе, 10 колонок) и с сохранением значений селектов, отсутствие секции у Fighter'а.

**2026-09-06 (Abyss Enhancement и каталог фитов SCA — последние пункты спецификации):** содержание листа закрыто целиком.

- **Abyss Enhancement** (`06-equipment.md`, Core II) — подсекция внутри «Снаряжения»: предмет, тип усиления, примечание и проклятие. Усиления хранятся **на самом предмете** (`weapon.abyss`, `armor.abyss`, `shield.abyss`), а не отдельным списком со ссылками: удалённое оружие не оставляет висячих строк. Список типов усиления зависит от того, чем усиливают — у щита есть `Evasion +1`, у оружия `Critical Threshold -1`.
- **Проклятие — один `<select>` на 36 строк** книжной таблицы 6×6: выбирается бросок, а имя проклятия дописывается рядом и хранится вместе с ним, чтобы лист печатал «3-4 — Near Death», а не голый бросок.
- **Лимит «двух на предмет» — в UI, не в схеме.** `.max(2)` в Zod означал бы, что импорт с тремя усилениями не проходит валидацию, а неудачный парсинг **выбрасывает персонажа целиком**. Схема остаётся снисходительной, кнопка «Добавить усиление» — нет.
- **Числа предмета усиления не двигают.** Игрок и так вписывает бонусы в поля оружия руками; автоматизировать половину типов (а половина условная — «vs Category», «если используется как magical implement») значило бы получить двойной счёт. Записываем, что несёт предмет, и только.
- **Каталог фитов SCA** (`src/data/combat-feats.ts`, 94 штуки из Core I и Core II) — и `<datalist>` на поле названия (тот самый «автокомплит» из спецификации), и выбор из каталога с автоподстановкой категории. Свободный ввод остаётся: фиты Core III (pp. 199–205) ещё не разобраны, да и книжные имена вида `Weapon Proficiency A/**` игрок дописывает под свою категорию оружия.

*Две находки браузерной проверки, обе про печать и обе чинят уже существовавшее:*
- `<input list=…>` рисует в Chrome стрелку выбора, и на бумаге она садилась посреди данных. Скрыта через `::-webkit-calendar-picker-indicator` — заодно чинит поля инвентаря.
- **Placeholder печатался как данные:** пустое поле «Набор авантюриста» уходило на бумагу с текстом «rope, lantern, backpack, tools…», как будто игрок это написал. Теперь `::placeholder` на печати прозрачный — поле сохраняет высоту, но остаётся чистой строкой для записи от руки.

Проверки: 173/173 тестов (12 новых), `tsc -b`/`oxlint`/`vite build` зелёные. В браузере — два усиления на оружие и одно на щит через UI с проверкой сохранённого, разные списки усилений для оружия и щита, исчерпание лимита, добавление фитов из каталога с правильной категорией, 94 подсказки в datalist, светлая и тёмная темы, печать A4 без переполнения и без элементов управления, пустой блок Бездны уходит с бумаги.

**2026-09-06 (разбор багов после ревизии кода):** пять исправлений, четыре из них — про то, что лист врал или молчал.

- **HP/MP не были ограничены максимумом.** Полоска зажималась до 100%, число рядом — нет: `30 / 23` выглядело нормально. Теперь значение зажимается и при записи, и при показе (сохранённое переживает максимум, который его оправдывал). У HP нет нижней границы — ниже нуля это законное состояние, ради него и существует Death Check; у MP нижняя граница 0.
- **Удаление активного персонажа выкидывало в форму создания.** `activeId = null` — это способ открыть форму по кнопке «Новый персонаж», и удаление его переиспользовало: игрок оказывался в форме создания, а над ней висел полный ростер. Теперь переход к тому, кто остался.
- **Отказ записи в localStorage пропадал молча.** Квота — несколько мегабайт на весь origin, а приватное окно может отказать вовсе; `setItem` бросал исключение в середину React-обновления. Теперь оба хранилища (персонажи и активный id) ловят отказ, шлют событие `sw25:storage-error`, и шапка показывает `role="alert"` с советом выгрузить персонажа в файл. **Тест на квоту сразу нашёл, что защищено было только одно хранилище из двух.**
- **Длинный текст пропадал с бумаги.** `<input>` не переносит строку, а печатные таблицы фиксированной ширины: примечание к предмету обрезалось краем ячейки без следа. Свободный текст в таблицах теперь через `PrintableField` — поле для экрана и печатный двойник-`<span>`, который переносится (и выровнен влево, как текст в поле, а не по центру ячейки).
- **Секции листа получили доступные имена** (`aria-labelledby` на `<section>` от собственного заголовка). Три кнопки «Add by hand» в разных секциях звучали одинаково; теперь каждая секция — landmark, и кнопка читается в контексте своей секции.

*Оставлено как есть:* пустые секции печатают строку-заглушку («No spells recorded yet»). Это осознанное решение, записанное в комментарии к print-правилам: заголовок с пустым местом — место, куда дописать от руки, в отличие от пустых сабблоков, у которых всё содержимое — скрытая кнопка «Добавить».

Проверки: 180/180 тестов (7 новых), `tsc -b`/`oxlint`/`vite build` зелёные. В браузере — зажим HP по максимуму и сохранение отрицательного HP, пять длинных значений целиком на бумаге (примечание к предмету, примечание Бездны, имя оружия, имя фита, заметка к заклинанию), печатный двойник невидим на экране, баннер отказа записи при подменённом `localStorage.setItem`.

**2026-09-06 (мелкая уборка: требования по силе, слоты SCA, поиск по каталогу):** три места, где лист знал ответ, но молчал.

- **Требования по силе.** Оружие, броня и щит с Min STR выше силы персонажа помечаются: число краснеет и рядом встаёт «⚠» с расшифровкой в подсказке («Requires STR 18, this character has 10»). Цветом одним обойтись нельзя — глиф несёт смысл сам, и он же уходит на бумагу. **Fencer халвит требование только оружия** (`06-equipment.md`); броня и щит остаются как есть, иначе половина фехтовальщиков получала бы ложные предупреждения. Книга не говорит, как округлять половину нечётного, — округляем вверх (строгое чтение), это записано в `requirements.ts`.
- Заодно у **щита появилось поле Min STR**: в схеме оно было с самого начала, а редактировать его на листе было негде.
- **Слоты SCA.** В шапке секции «Способностей взято: 2 / 2» — слотов `floor((уровень авантюриста + 1) / 2)`, то есть один при создании и по одному на каждом нечётном уровне (слот создания и слот 1-го уровня — один и тот же). **Auto-Acquired фиты не считаются**: они приходят с уровнем класса и слот не занимают. Перебор красится как перерасход опыта, но ничего не запрещает — лист часто заполняют на уровень вперёд.
- **Поиск по каталогу заклинаний.** 257 штук в одном `<select>` — это прокрутка, а не выбор. Фильтр по части названия, группировка по кругам сохраняется (чтобы отфильтрованный список по-прежнему показывал, что выше уровня класса), при пустом результате — строка «ничего не найдено» вместо пустого списка. Поле поиска — часть `controlRow`, на бумагу не идёт.

Проверки: 200/200 тестов (20 новых, из них 9 — на чистые функции `requirements.ts`), `tsc -b`/`oxlint`/`vite build` зелёные. В браузере — три предупреждения по силе у персонажа с STR 10 и исчезновение оружейного при смене класса на Fencer (броня и щит остаются), «2 / 2» при трёх фитах с одним автоматическим, сужение каталога с 40 вариантов до 2 по запросу «bolt» и добавление найденного, печать A4 без переполнения и со скрытым полем поиска.

**2026-09-06 (Check Packages — стандартные значения, которые игрок складывал в уме):** секция «Боевые параметры» перестала быть только боевой.

- **Пять пакетов проверок** (`04-combat-and-scas.md`, «Check Packages»): Initiative, First Aid, Danger Sense / Observation, Monster Knowledge, Healing Power. Каждый — «уровень класса + модификатор характеристики», как и боевые значения рядом.
- **Universal против классовых.** Initiative и First Aid есть у всех (Initiative без Scout/Tactician — один модификатор AGI, «базовая инициатива» из доков; First Aid — уровень авантюриста + DEX). Danger Sense, Monster Knowledge и Healing Power показываются только при Scout / Sage / Priest: строка «Monster Knowledge 2» на листе без Sage была бы враньём.
- **Tactician считается для Initiative, но не для Danger Sense** — по докам уровень к инициативе добавляют оба класса, а чувство опасности принадлежит только Scout.
- **Movement сознательно не сделан.** В доках у него нет формулы, только «зависит от расы и снаряжения»; выдуманное число хуже отсутствующего. Записано тестом, чтобы следующая сессия не «дочинила» это по невнимательности.
- **Секция больше не исчезает** у персонажа без боевого и волшебного класса: раньше `return null` уносил вместе с пустыми таблицами и универсальные проверки, которые есть у всех. Заодно у `CombatStatsSection` появились тесты — она была одной из трёх непокрытых.

*Формула инициативы — сведение двух мест в доках:* таблица производных даёт «Scout/Tactician Level (если есть)», а список универсальных проверок относит Initiative к AGI-основанным. Взято и то, и другое: уровень плюс модификатор AGI, без класса — модификатор один. Обоснование записано в `derived-stats.ts`.

Проверки: 213/213 тестов (13 новых: 6 на чистые функции, 7 на секцию), `tsc -b`/`oxlint`/`vite build` зелёные. В браузере — Fighter видит две универсальные строки, связка Scout 4 / Sage 5 / Priest 2 даёт все пять с верными значениями (6 / 7 / 6 / 7 / 4), RU-подписи, светлая и тёмная темы, печать A4 без переполнения.

**2026-09-06 (справочник — экран для выбора):** всё, что нужно, чтобы выбрать расу и класс, лежало в данных и не показывалось.

- **Форма создания показывала только названия.** Кости коррекции жили исключительно в `title=`-подсказке (не видна с тача, не видна с клавиатуры, всплывает уже после неверного ввода), запрещённые классы не показывались нигде, расовые способности — только на листе уже созданного персонажа.
- **Семь вкладок** в `src/features/reference/`: расы (кости с диапазоном броска, запреты, способности, книга), классы (тип, ранг, ключевые характеристики, школа, цена уровня, описание + полная таблица опыта 1–15), предыстории (по расе и кросс-расовый индекс), заклинания с поиском, фиты, техники и песни, предметы и проклятия Бездны.
- **Карточка расы в форме создания** — тот же компонент, что и в справочнике: выбор делается там, и уходить за справкой на другой экран неудобно. Ничего не дублирует из селектов: ни названия предыстории, ни диапазона броска, ни опыта.
- **Проза только у классов и только в i18n.** Запрет на прозу в `src/data/` сформулирован как двуязычный аргумент — «половина английского каталога хуже, чем никакого». У классов покрытие полное (24 из 24 в `02-classes.md`), поэтому RU оттуда плюс английский перевод легли в файлы локалей, а данные остались локаль-нейтральными. У рас проза есть у 12 из 18 — это ровно та половина, от которой проект отказывался, поэтому расы показывают только структурные факты.
- **`classes.ts` получил `keyAbilities`** — термин, а не проза. У волшебных классов в книге в этом месте школа магии, а не характеристика, поэтому девять волшебных несут INT и SPR, **выведенные из формул**, которые тот же док печатает над таблицей (Magic Power → INT, MP max → SPR) и которые приложение уже считает. В коде и в тесте это помечено как вывод, а не как колонка из книги.
- **Кросс-расовый индекс предысторий** (`src/lib/backgrounds.ts`, туда же переехал `listBackgroundOptions`): «Cleric» у Human это 4/8/9, у Elf 9/5/12, у Dwarf 4/7/9 — **строка на каждую расу, объединять нельзя**, объединённая была бы тихим враньём. Есть и обратный индекс: какие расы могут стартовать с данным классом.
- **Печать справочника оставлена как есть и полезна сама по себе** (распечатанный список заклинаний): вкладки, ростер и пикеры — кнопки и селекты в шапке — с бумаги уходят сами, печатается ровно открытый каталог.

*Побочно:* тест на паритет ключей локалей (`src/i18n/locales.test.ts`) заодно проверяет, что описание класса не осталось одинаковым в обеих локалях — копипаста английского в `ru.json` прошла бы все остальные проверки. Плюрализация из проверки исключена намеренно: у русского `_few`/`_many`, у английского `_other`, и это не расхождение, а требование i18next.

Проверки: 251/251 тестов (39 новых), `tsc -b`/`oxlint`/`vite build` зелёные. В браузере — все семь вкладок, сверка значений (Dwarf SPR 2d6+6 (8–18), Priest в запрете у Tabbit, Vagrant-пометка у Alv, Major 1000 против Minor 500, Cleric тремя разными строками по расам, сужение каталога заклинаний по «bolt»), смена локали меняет описание класса, тёмная тема, печать вкладки заклинаний без переполнения и без элементов управления, карточка расы в форме создания.

---

## Сводка по источникам (какая книга что даёт)

Все PDF — community EN-переводы, лежат в `files/`. Метод проверки: полнотекстовое извлечение через `pymupdf` + поиск по оглавлению/ключевым словам (не по скриншотам).

| Книга | Статус | Ключевые находки | Куда записано |
|---|---|---|---|
| **Core Rulebook I** | ✅ Разобрана полностью | 7 рас (кости A–F, Background-таблицы), заклинания Truespeech/Spiritualism/Divine/Magitech ур.1–6, **полный список Combat Feats/SCA** (pp.249–265) | `01-races.md`, `09-spells-core1.md`, `04-combat-and-scas.md` |
| **Core Rulebook II** | ✅ Разобрана полностью | 3 расы (Lildraken/Grassrunner/Meria), классы Fairy Tamer/Enhancer/Bard, заклинания ур.7–10, **Adventurer Rank Chart с точными порогами Reputation** (pp.110–115), Abyss Enhancement, ~47 новых Combat Feats | `01-races.md`, `09/10-spells-core*.md`, `02-classes.md`, `06-equipment.md`, `04-combat-and-scas.md` |
| **Core Rulebook III** | 🟡 Классы и расы закрыты, мир/предметы — нет | 2 расы (Tiens/Leprechaun) + Enhanced Racial Abilities Lv11+ для всех 12 рас, XP-таблица до 15 ур. **Rider** (Mount Rules, ~30 Stunts) и **Alchemist** (Evocations, Material Cards) — полная механика подтверждена 2026-07-09. Не задокументированы: новые Combat Feats, снаряжение/mounts data, боги "Second Sword", бестиарий | `01-races.md`, `02-classes.md` |
| **Outlaw Profile Book** | ✅ Разобрана (для целей листа) | 4 расы (Alv/Shadow/Soleil/Weakling + 4 варианта Weakling) — описание/языки/способности. **Без костей A–F** — расы созданы для альтернативной системы персонажа Vagrant (не-авантюрист), не для стандартного создания | `01-races.md` |
| **Epic Treasury** | ✅ Проверена | Work Skills (проф. навыки для ролплея), Point Buy Character Creation, Advanced Combat (2D grid), Mount Data, Average Height by Race. **НЕ содержит** General Skills/Tactician/Druid (ошибочно считалось ранее) | `07-general-skills-and-misc.md`, `01-races.md` |
| **Raxia Life** | ✅ Проверена | Расширение Work Skills (по локациям); бонус — расы **Abyssborn/Newman** (реприз из Arcane Relic, только описание+способности). **НЕ содержит** General Skills | `01-races.md`, `07-general-skills-and-misc.md` |
| **Magus Arts** | ✅ Geomancer + Tactician закрыты | Полная механика: Geomancer (Aspects/Qi/Geograph), Tactician (Stratagems/Maneuvers/Edge). Druid/Daemonologist — только лор. Бонусы (не задокументированы): Deep Magic, Stunts, Evocations, боги "Second Sword", расширенная Fairy Magic | `02-classes.md` |
| **Monstrous Lore** | ✅ Разобрана полностью | Druid (Nature Magic, ~59 заклинаний, Symbols of Beneficence/Symbolic Lore) и Daemonologist/Summoner (Summoning Arts, ~46 заклинаний, Gate Imp/Abyss Gate/Daemon Action Chart/Banishment) — оба Major-класса с полной механикой. Плюс: New Backgrounds, ~400 монстров, Golem Enhancing Items, Familiar Data | `02-classes.md` |
| **Abyss Breaker** | ✅ Разобрана полностью | Abyss Gazer (Wizard-type, Abyssal Magic, ~27 заклинаний с системой Enhancement через Daemon's Blood/Abyss Shards — **подтверждён отдельным классом от Daemonologist**) и Dark Hunter (Other-type, Essence Weaving, ~28 штук, Mental Power=Lv+SPR). Плюс Thrown Weapons (B/A/S/SS), Abyss Skills/Abyss Corruption (риск потери PC как Daemon) | `02-classes.md`, `06-equipment.md` |
| **Tyrants Crypts** | 🟡 Разобрана (только Preview, 20 стр.) | Bibliomancer (Wizard-type, Arcane Magic) — полная механика класса: Grimoire, Prepared/Emergency Spell Slots (табл. ур.1–15), Critical Failure Value, заклинания покупаются/находятся поштучно. Список заклинаний и весь остальной контент книги — под эмбарго переводчиков | `02-classes.md` |
| **Arcane Relic** | ⬜ Не скачана | Полные данные (кости A–F) для Abyssborn/Newman — сейчас есть только реприз-описание из Raxia Life | — |

### Важные коррекции (запомнить на будущее)

- **General Skills (72+) как отдельная механика листа, вероятно, не существует.** Проверено в 4 источниках (Core I, Core II, Epic Treasury, Raxia Life) — термина нет нигде. Изначальное допущение — ошибка раннего (доpymupdf) исследования. Не тратить время на дальнейший поиск без новых оснований.
- **Tactician и Geomancer** — правильный источник **Magus Arts** (не Epic Treasury, не несуществующая книга "Mage Arts" — опечатка/путаница из старых заметок).
- **Druid и Daemonologist (Summoner/Daemon Tamer)** — подтверждённый источник **Monstrous Lore** (не Epic Treasury, не Magus Arts — там только лор-комментарий). Полная механика (Nature Magic / Summoning Arts) → `02-classes.md`.
- **Abyss Gazer** (Abyss Breaker) — подтверждён **отдельный класс от Daemonologist**, не синоним (разные школы: Abyssal Magic ≠ Summoning Arts, нет Gate Imp/Abyss Gate/призыва). Счётчик классов скорректирован с 23 до **24**.
- **Базовый Abyss Enhancement уже был задокументирован из Core Rulebook II** (`06-equipment.md`) — Abyss Breaker его только переиздаёт для справки, ничего нового. Новый контент там — **Abyss Skills/Abyss Corruption** (именные способности вместо типовых бонусов + риск постоянных штрафов вплоть до потери персонажа как Daemon).
- **Bibliomancer** (Tyrants Crypts) — подтверждён, Wizard-Type Major, школа **Arcane Magic** (не "Book Magic", как формулировалось ранее). Уникальная механика: заклинания не даются автоматически по уровню, покупаются/находятся поштучно и готовятся в Grimoire через слоты.
- Полный список из 8 дополнений сообщества (Outlaw Profile Book, Epic Treasury, Raxia Life, Magus Arts, Monstrous Lore, Abyss Breaker, Tyrants Crypts) разобран — **книжный ресёрч по классам закрыт**. Незакрыт только контент под эмбарго (Tyrants Crypts полностью) и книга Arcane Relic (не скачана, нужна только для костей A–F Abyssborn/Newman).
- **Rider и Alchemist (Core Rulebook III)** — полная механика подтверждена (pp.84–113, 190–198). Это закрывает последний гэп по классам самого Core III (все 24 класса SW2.5 теперь имеют полную механику, кроме списка заклинаний Bibliomancer под эмбарго).
- Найдена **community-библиотека всех переводов SW2.5** — общая папка Google Drive проекта Sword World Translation Project (прямые ссылки на файлы уже использованы для скачивания). Справочная вики `sw25.wikidot.com` существует, но геоблокирована для RU/BY — недоступна напрямую (WebFetch/curl), только через WebSearch-сниппеты.

---

## Что известно о листе (итоги разбора)

24 класса, 18 рас (Core I–III + Outlaw + Abyssborn/Newman), 12+ магических школ, Adventurer Level cap = 15. Ключевые формулы подтверждены:

- `HP = Adv.Level × 3 + VIT`
- `MP = Σ Wizard Class Levels × 3 + SPR`
- `Modifier = floor(stat / 6)`
- `DEX = Skill + A`, `AGI = Skill + B`, `STR = Body + C`, `VIT = Body + D`, `INT = Mind + E`, `SPR = Mind + F`
- `Bardic Power = Bard Level + INT modifier` (для Finales)
- `Enhancer SV = Enhancer Level + INT modifier` (для Fire Breath)

Полная структура: `docs/sheet-content/00-index.md`

**Заклинания Core I (подтверждено):**
- Truespeech: 18 (кр. 1–6) · Spiritualism: 21 (кр. 1–6) · Divine Basic: 20 (кр. 1–6) + 16 Specialized (8 богов × 2) · Magitech: 23 (кр. 1–6)

**Заклинания Core II (подтверждено):**
- Truespeech: +16 → 34 · Spiritualism: +16 → 37 · Divine Basic: +12 → 32 · Divine Specialized: 13 богов, 48 заклинаний всего · Magitech: +14 → ~37 · Fairy Magic: 70 (7×10) · Techniques: 20 · Spellsongs: 17 · Finales: 8

---

## Открытые вопросы по продукту

Стек/фичи/легальность закрыты 2026-07-07 (`docs/tech-stack.md`, `docs/features.md`, `docs/legal.md`): Vite + CSS Modules + Radix UI + Jotai + Zod, GitHub Pages (приватный репо), умная автокалькуляция, dice roller вне MVP, несколько персонажей в localStorage, печать через "Скачать PDF", проект приватный. Дополнено 2026-08-04: HP/MP трекер + статус-эффекты в MVP, тестирование — Vitest + Testing Library.

Остаётся открытым:
1. **Риск GitHub Pages без Pro/Team** (публичность репозитория/сайта) — найден при аудите 2026-08-04, **не решён**. Блокирует деплой, но не разработку.
2. **Нужен ли домен** для GitHub Pages деплоя? — не обсуждалось повторно.
3. **Настоящее скачивание PDF одной кнопкой** — сейчас печать идёт через диалог браузера («Сохранить как PDF»). Требует решения, стоит ли это `@react-pdf/renderer` с дублирующей вёрсткой (разбор — в хронологии за 2026-08-04).
4. Мелкие вопросы дизайна: набор иконок и формальная проверка контраста/WCAG (см. `docs/design.md`). ~~Аватар персонажа~~ — сделан 2026-08-12 (загрузка с уменьшением до 320 px).

Закрыто по ходу разработки 2026-08-04 (детали — в хронологии сессии выше):
- ~~Миграция JSON при старой `schemaVersion`~~ — решено: `CharacterSchema` c `.default(...)` на новых полях + `CharacterSchema.safeParse` при каждом чтении из `localStorage`/импорте (silent backfill, не отдельный versioned-migration runner — этого хватило, т.к. пока все изменения были только "добавление новых полей с дефолтом", не переименования/breaking changes).
- ~~Схема статус-эффектов~~ — решено: фиксированный enum полей (6 характеристик + accuracy/evasion/fortitude/willpower/defense/actionCheck + `custom`), раунды — независимый счётчик на каждый эффект, пресетов нет (только ручной ввод).
- ~~Визуальная концепция листа~~ — решено пользователем: современный clean UI, один длинный скролл, десктоп-first но responsive, в RU-локали «Русский (English)». Все решения и то, что осознанно не делалось, — в `docs/design.md`.

---

## Открытые пробелы в контенте

1. Список заклинаний Arcane Magic, 8 Work Skills, Dig Arts, Magic Items, монстры — **Tyrants Crypts под эмбарго переводчиков**, недоступно без покупки оригинала
2. Детальные механики Conjurer Golems/Undead — Core II pp.87–93 (доп. Golem Enhancing Items уже найдены в Monstrous Lore pp.225–235)
3. Magitech кр.7–10 — имена подтверждены, детальные механики некоторых нет
4. Некоторые Specialized Divine Core I (Daybreak/Tidan 10 и др.)
5. Core III прочий контент — остались только боги «Second Sword» и бестиарий (Part 4–5), листу персонажа не нужны. Combat Feats (pp. 199–205) закрыты 2026-09-06, снаряжение (pp. 206–249) и маунты (pp. 247–262) — там же. Не разобран построчно список Stunts класса Rider (pp. 84–106)
6. Magus Arts Stunts (pp.173–177) — соотношение с версией Core III построчно не сверено. Evocations (pp.181–184) сверены 2026-09-06: точная перепечатка Core III
7. Deep Magic (новая школа, Magus Arts, требует Sorcerer+Conjurer)
8. Расширенная Fairy Magic (3-type/6-type selection, Chaos-заклинания, Magus Arts)
9. Система Vagrant (Outlaw Profile Book) — D66 таблицы для не-авантюристов, вне MVP
10. Point Buy/Advanced Combat/Treasure Drop (Epic Treasury) — опциональные post-MVP системы
11. Кости A–F для Abyssborn/Newman — нужна книга Arcane Relic (не скачана; единственная известная недостающая книга)
12. ~~Построчные списки Nature Magic, Summoning Arts, Abyssal Magic и Evocation Data~~ — сделаны 2026-09-06 (`13-spells-supplements.md`, `14-evocations.md`). Осталось из этого пункта только одно: названия Basic Fairy Magic, помеченные в `11-fairy-magic.md` как требующие верификации по PDF
13. Abyss Breaker Part 2–4 (World lore, Random Shallow Abyss generation, Monster Data 80+ монстров) — не приоритет для листа персонажа
14. War Leader — возможно, никогда не существовал как класс (не встретился ни в одной из 8 разобранных книг), аналогично прежней General Skills-гипотезе

**2026-09-06 (каталоги Nature / Summoning / Abyssal Magic):** закрыт первый пункт списка «что дальше» — три школы магии из дополнений, у которых в доках были только механика и оценка количества.

- **Уровень заклинания в PDF не лежит текстом.** `pdftotext` вытаскивает название, стоимость и весь текст эффекта, но круг нарисован графикой — полосой «3rd Level Nature Spells» над колонкой и кружком-значком слева от названия. То же и в Core I/II. Поэтому страницы отрисованы в PNG (Chromium через Edge + pdf.js) и круги сняты глазами с картинок, а названия и MP — из текстового слоя. Скрипт сохранён как `scripts/render-pdf-pages.mjs` с инструкцией внутри: он ещё понадобится для фитов Core III и Evocations.
- **Что добавлено:** `src/data/spells/nature.ts` (60 заклинаний, 15 уровней по 4), `summoning.ts` (46: по 3 на уровень, на 2-м — 4), `abyssal.ts` (35: по 2–3 на уровень). Каталог вырос с 257 до **398**, школы добавлены в `CATALOGUED_SCHOOLS`. Построчные списки записаны в `docs/sheet-content/13-spells-supplements.md`.
- **Оценки в доках были прикидками, а не подсчётом:** Nature оказалось 60 (стояло «~59»), Abyssal — 35 (стояло «~27»). Цифры в `02-classes.md` и `00-index.md` исправлены по факту.
- **Круги 11–15 не показывались бы вообще.** Пикер заклинаний строил группы по захардкоженному `[1..10]` — для Core I/II этого хватало, а половина Nature Magic молча исчезла бы из списка. Группы теперь берутся из самих данных (`circlesIn`). Тест «каждое заклинание внутри напечатанных кругов» тоже знал только про 10 и теперь различает школы Core (до 10) и дополнений (до 15).
- Тест ручного ввода для школы без каталога переехал с Друида (у него каталог появился) на Библиоманта — Arcane Magic осталась единственной школой без построчного списка.
- Проверено в браузере: у Друида 15-го уровня в пикере все 15 кругов и 60 заклинаний, у Abyss Gazer 12-го круги 13–15 помечены «выше уровня класса», справочник показывает восемь школ и 46 строк Summoning Arts, печать листа с заклинанием 15-го круга чистая, консоль пустая.

**2026-09-06 (фиты Core III):** закрыт следующий пункт того же списка — 27 боевых фитов из Core Rulebook III, pp. 199–205, которых не было ни в доках, ни в каталоге.

- Здесь книжный текст оказался обычным: страницы одноколоночные, `pdftotext -layout` отдал названия, требования, «Use/Appl./Risk» и эффекты целиком; отрисовка страницы понадобилась только на сверку, что ничего не пропущено между разворотами.
- 12 selectively passive, 7 active (в терминах листа — declaration), 8 automatically acquired. Большинство — третья ступень цепочек Core I/II (Aimed Attack III, Power Strike III, Lethal Strike III, Armor Piercer III, Violentcast II, Critical Cast II, Pinpoint Attack II, Powerful Magic II, Ever-Changing II, Additional Songs III), поэтому к ним применимо то же правило замены, что уже описано для Core II. Отдельная группа — четыре фита Алхимика вокруг Evocations и Material Cards.
- Каталог вырос с 94 до **121**; полные таблицы с требованиями и эффектами — в `04-combat-and-scas.md`. У каталога фитов не было ни одного теста — добавлен `src/data/combat-feats.test.ts` (счётчики по книгам, уникальность id, покрытие категорий).
- Проверено в браузере: 121 фит в пикере, добавление Power Strike III, счётчик слотов «1 / 8», справочник показывает все 121 строку, консоль пустая.

**2026-09-06 (Evocations Алхимика):** § 1.1 плана — 26 Evocations из Core III pp. 190–198 и своя секция на листе.

- **Сверка двух источников (открытый вопрос с июля):** Magus Arts pp. 181–184 — точная перепечатка Core III, те же 26 названий, карты и эффекты. Половина пункта 6 «Открытых пробелов» закрыта; Stunts построчно не сверялись.
- **Почему не строка в списке заклинаний:** ресурс — Material Cards (5 цветов × 4 ранга B/A/S/SS), ранг выбирается за столом и задаёт силу эффекта, MP не тратятся вовсе. Отсюда отдельная `EvocationsSection` рядом с `ArtsSection`, видимая только у Алхимика.
- **Что в данных:** `src/data/evocations.ts` — название, требуемый уровень класса (1/5/10), карты и значки ▶▶ / △. Значки нарисованы графикой — сняты с отрисованных страниц; сами уровни, в отличие от заклинаний, лежат текстом («1st Level Alchemist Required»). Оценка «~28» в доках снова оказалась прикидкой: по книге 26.
- **Схема:** `evocations` (список) и `materialCards` (запас, ключ «цвет-ранг») через `.default(...)`. Запас — записью, а не жёсткой сеткой 5×4: неизвестный цвет или ранг не должен ронять чтение сохранённого персонажа.
- **На листе:** SV проверки (уровень Алхимика + модификатор INT, новая `evocationPower`), счётчик «изучено N из уровня класса» (книга даёт 1 Evocation за уровень), таблица известных Evocations и сетка запаса карт со стоимостью в гамелах. Сетка сделана таблицей осознанно — двадцать отдельных полей развалились бы на печати.
- Восьмая вкладка справочника + ключи локалей en/ru.
- Проверено в браузере: 26 позиций в пикере, три уровня-группы («Level 5 — above class level» у Алхимика 3-го уровня), добавление Heal Spray/Dispel Needle/Combine Materials с картами и значками, запас 2 × Gold SS + 12 × Green B = 40 240 г, SV 12 у Алхимика 10-го уровня, печать секции чистая (пикер не печатается, ▶▶/△ на бумаге видны), консоль пустая.
- **Про параллельную работу:** пачка по снаряжению Core III писалась в том же дереве одновременно с этой; `tsc -b` был временно красный (`equipment.ts` импортировал ещё не добавленный тип `EquipmentRank`). Сейчас всё сведено: 309/309 тестов, `tsc -b`/`oxlint`/`vite build` зелёные.

---

**2026-09-06 (снаряжение Core III):** первая половина § 1.4 плана — pp. 206–249, последний неразобранный кусок Core-книг.

- **Crit Value текстом не лежит.** Ровно та же беда, что с кругами заклинаний: значение нарисовано кружком (⑩/⑪/⑫), текстовый слой его не отдаёт. Страницы 207–220 отрисованы в PNG, Crit снят глазами. Выяснилось и хуже: в узких SS-таблицах (Flails, Wrestling) `pdftotext` **перемешивает значения между соседними строками** — по тексту у Quadblaze были чужие числа. Все цифры сверены с картинкой.
- **Две ошибки самой книги.** Ring of Righteous Belief стоит 30,000 в сводной таблице (p. 224) и 8,000 на карточке предмета (p. 243) — обе страницы проверены по отрисовке, взята карточка, расхождение помечено в каталоге. У Heart Tracker сноска ведёт на p. 230, где его карточки нет вообще.
- **Что в данных:** `src/data/equipment.ts` — 63 оружия (со строками на каждый хват: 1HR для боя верхом, 2Hs/2Hp — рубящий и колющий), 16 брони, 9 щитов, 45 предметов общего снаряжения. Каталог **частичный и это осознанно**: Core III дописывает к спискам Core I–II, а те в доках никогда не разбирались, поэтому Longsword по-прежнему вводится руками.
- **На листе:** пикеры оружия/брони/щита, две новые колонки в таблице оружия (Range и заметка — раньше поля `range` и `notes` были в схеме и не показывались), ранг и заметка у щита (`notes`/`rank` в `ShieldSchema` через `.default(...)`), подсказки общего снаряжения в инвентаре. Девятая вкладка справочника.
- **Найдено попутно, исправлено:** (1) `KnownSpellSchema` резала круг заклинания на 10, а каталоги дополнений доходят до 15 — пикер спокойно добавлял такое заклинание, а следующее чтение из localStorage роняло **всего персонажа**; отдельный коммит и регрессионный тест. (2) В селекте ранга брони был захардкожен B/A/S — броня Core III почти вся SS, и селект рендерился пустым, не совпадая со своим значением.
- **Грабли на печати.** Таблица оружия выросла с 11 колонок до 13, и Min STR «20» стал печататься как обрезанная «2»: место съедали стрелки `input[type=number]`. Сначала поставил `min-width` на таблицу — **не помогло и оказалось лишним** (собственный минимум таблицы и так больше); реальное лечение — убрать стрелки внутри табличных ячеек, как это уже делает `print.css` для бумаги. На A4 13 колонок помещаются.
- Проверено в браузере (Edge + Playwright): добавление Normal Lance строкой 1HR, Desperado с Power 0 и «Magazine 2», Imperial ранга SS, Knight Shield с пометкой Mount Protection, справочник на 63 строки, RU-локаль, печать в настоящий A4 PDF — чисто, консоль пустая.

**2026-09-06 (маунты Core III):** вторая половина § 1.4 — pp. 247–262. Классу Rider стало чем пользоваться.

- **Маунт — не предмет, а мини-монстр.** Своя атака, Evasion, Defense, HP, MP и сопротивления, и все они меняются с уровнем маунта, а уровень равен Adventurer Level Jockey-я, зажатому в Appropriate Level. Поэтому в данных не строка на маунта, а таблица «уровень × секция»: 17 маунтов, 7 вариантов за репутацию (те же звери с +5 к движению, без собственного блока данных), 27 предметов снаряжения маунта.
- **Текстовый слой снова врёт на многосекционных.** У Tilgris, Draconet и Lesser Dragon `pdftotext` сдвигает колонку Level относительно строк секций — по тексту выходило, что у 9-го уровня данные 8-го. Эти три таблицы сняты с отрисованных страниц 258–260.
- **Схема:** `mounts` со списком секций у каждого маунта. Fortitude и Willpower есть только у главной секции — книга бросает сопротивление один раз на всю тушу; тест это и проверяет.
- **На листе:** секция Mounts, видимая при уровнях Rider или если маунт уже записан (без класса ездить можно только на Horse, War Horse, Mini Manabike и Manabike). Выбор из каталога заполняет блок на посчитанном уровне; смена уровня **или контракта** пересчитывает его заново — Proprietary Contract даёт +10 Max HP каждой секции. HP каждой секции отслеживается отдельно: секция на 0 HP отключается. Маунт ниже минимального Appropriate Level в пикере disabled, а не спрятан.
- **Заголовки таблицы пришлось сократить** до Fort/Will/Weapon/Armor: на 8.5pt «Fortitude» и «Willpower» налезали друг на друга в узких колонках. Книга их тоже сокращает.
- Десятая вкладка справочника: блок маунта по уровням, варианты за репутацию, четыре таблицы снаряжения маунта.
- Проверено в браузере: Tilgris на 10-м уровне с двумя секциями (93/103 HP после Proprietary Contract), Manabike 6-го уровня без MP, оружие и броня по секциям, справочник с девятью строками Lesser Dragon, печать A4 — чисто, консоль пустая.
- **Чего Rider-у не хватает:** построчного списка ~30 Stunts (Core III pp. 84–106, вне разобранного диапазона). Поставлено в план отдельным пунктом § 1.5.

---


## Следующий шаг — для новой сессии

Книжный ресёрч закрыт с июля. **Спецификация листа закрыта полностью.** Ветка `master`, всё закоммичено и запушено, рабочее дерево чистое. 309/309 тестов, `tsc -b`/`oxlint`/`vite build` зелёные. Последние коммиты: `b2dd775` (круг заклинания до 15 — потеря персонажа) и `8621412` (снаряжение, маунты и Evocations Core III одной пачкой — почему одной, см. про две сессии ниже).

За сессию 2026-09-06 сделано двенадцать пачек: инвентарь расходников · техники, песни и финалы · Abyss Enhancement и каталог фитов SCA · разбор багов (HP/MP, удаление активного персонажа, отказ localStorage, обрезание текста на печати, доступные имена секций) · мелкая уборка (требования по силе, слоты SCA, поиск по каталогу) · пакеты проверок · экран-справочник · каталоги Nature/Summoning/Abyssal Magic · фиты Core III · Evocations Алхимика · снаряжение Core III · маунты Core III. Подробности каждой — в хронологии выше.

**Разбор Core-книг закрыт.** Из Core Rulebook III остались только боги «Second Sword» и бестиарий — ни то, ни другое листу персонажа не нужно. Единственное, чего не хватает конкретному классу, — построчный список Stunts у Rider (pp. 84–106), он поставлен в план § 1.5.

Что приложение умеет на сегодня:

- **Создание персонажа** — раса → предыстория с группировкой по таблицам → характеристики с проверкой диапазона коррекции, плюс карточка расы с костями, запретами и способностями прямо в форме.
- **Лист** — характеристики с журналом прироста, классы с покупкой уровней за опыт, репутация и ранг, боевые параметры и пакеты проверок (Initiative, First Aid, Danger Sense, Monster Knowledge, Healing Power), снаряжение с инвентарём, боеприпасами, журналом трат, предупреждениями по силе и Abyss Enhancement, заклинания (каталог 398 + поиск), техники и песни Enhancer/Bard (каталог 45), SCA со счётчиком слотов и каталогом на 121 фит, Evocations Алхимика с запасом Material Cards, маунты Rider-а (каталог 17, свои HP по секциям), статус-эффекты, портрет, заметки и связи.
- **Справочник** — десять вкладок: расы, классы, предыстории (по расе и кросс-расовый индекс), заклинания, фиты, техники, Evocations, снаряжение Core III, маунты, предметы и проклятия Бездны.
- **Прочее** — HP/MP-трекер, localStorage с сообщением при отказе записи, экспорт/импорт JSON, EN/RU, печать на A4.

**Из спецификации листа (`docs/sheet-content.md`) не осталось ничего.** Очередь дальнейших работ разложена в **`docs/roadmap.md`** — там же открытые решения и то, что заблокировано источниками. Коротко:
- **Stunts класса Rider** (Core III pp. 84–106, § 1.5) — единственное, чего конкретному классу не хватает на листе: данные маунтов есть, а половина их уникальных способностей помечена «Prerequisite: (Stunt)»
- Abyss Skills / Abyss Corruption (Abyss Breaker) — надстройка над сделанным Abyss Enhancement: именные способности вместо типовых бонусов и своя таблица проклятий
- Опциональные системы из «Открытых пробелов»: Vagrant, Point Buy, Advanced Combat, Treasure Drop, Work Skills, Fellow
- Мелочи, которые обсуждались и не делались: дублирование персонажа, экспорт всего ростера одним файлом, dice roller (вынесен за MVP решением пользователя)

**Пробелы каталога заклинаний** (не баги, а отсутствие источника): осталась одна школа — **Arcane Magic** (Bibliomancer, Tyrants Crypts под эмбарго переводчиков); плюс Basic Fairy Magic — док помечает названия как требующие верификации по PDF. Для них на листе работает ручной ввод. Nature / Summoning / Abyssal разобраны 2026-09-06 по PDF.

**Требуют вашего решения, а не работы:**
- **Приватность GitHub Pages** без Pro/Team — блокирует деплой (см. открытые вопросы выше)
- **One-click скачивание .pdf** — сейчас печать через диалог браузера. Цена настоящего файла по кнопке: `@react-pdf/renderer` с дублирующей вёрсткой листа, которая разъедется с реальной. Рекомендация прежняя — не делать
- Дизайн: набор иконок и формальная проверка контраста/WCAG (`docs/design.md`); аватар из этого списка сделан

Начинать сессию стоит с `git status`, чтения этого файла и `docs/roadmap.md` (что делать дальше) — весь код в `src/`, запуск через `npm install && npm run dev`.

**Заметка про проверку в браузере:** Playwright ставится разово (`npm install --no-save playwright`, в `package.json` не попадает). Свежая версия пакета просит Chromium новее скачанного — вместо докачки браузера проще запускать через установленный Edge: `chromium.launch({ channel: 'msedge' })`. Он ловит то, чего jsdom не видит в принципе: потерю персонажа из-за пустой строки снаряжения, экранный цвет полосок на печати, обрезание текста в textarea, неубираемый на бумаге `<select>`, доступное имя «LanguageENRU», а 2026-09-06 — стрелку datalist, печатающийся placeholder, дубли ключей React у одноимённых расовых способностей и кнопку-переключатель, которая переименовывалась и совпадала по имени с другой. Продолжать так проверять любой UI-код.

**Грабли разбора PDF (дополнено 2026-09-06 снаряжением и маунтами).** Текстовый слой врёт не только про графику:

- **Графикой нарисованы**: круг заклинания, значки ▶▶/△ у Evocations и **Crit Value у оружия** (кружки ⑧–⑫). Всё это снимается только с отрисованных страниц.
- **В узких таблицах `pdftotext -layout` перемешивает значения между соседними строками.** У SS-Rank Flails и SS-Rank Wrestling числа Quadblaze и Tyrant достались чужим предметам; у многосекционных маунтов (Tilgris, Draconet, Lesser Dragon) колонка Level сдвинута относительно строк секций, и по тексту выходит, что у 9-го уровня данные 8-го. **Любую таблицу уже ширины экрана сверять с картинкой, а не только там, где ждёшь графику.**
- **В книге бывают собственные ошибки.** Ring of Righteous Belief: 30,000 в сводной таблице (p. 224) против 8,000 на карточке предмета (p. 243). Heart Tracker: сноска «Details p. 230», а карточки там нет. Обе проверены по отрисовке — это не сбой извлечения. Решение записывать в док и в комментарий каталога, а не молча выбирать одну сторону.

**Грабли UI (дополнено 2026-09-06).**

- **Стрелки `input[type=number]` съедают ширину ячейки.** Таблица оружия выросла с 11 колонок до 13, и Min STR «20» стал рисоваться как обрезанная «2». Первым делом поставил таблице `min-width` — **не помогло и оказалось лишним**: собственный минимум таблицы и так был больше, `overflow-x` у `.tableWrap` и без того работал. Лечение — убрать стрелки внутри табличных ячеек (`appearance: textfield`), как это уже делал `print.css` для бумаги. Прежде чем чинить ширину, стоит измерить `scrollWidth`/`clientWidth` таблицы, а не гадать по скриншоту.
- **Заголовки таблицы на 8.5pt слипаются.** «Fortitude» и «Willpower» в узких колонках маунта налезли друг на друга на бумаге — сокращены до Fort/Will, как в самой книге.
- **Одинаковые доступные имена ломают и тесты, и доступность.** Три пикера «Из каталога» и три кнопки «Добавить» дали неоднозначные `getByLabelText`. Развёл на «Оружие/Броня/Щит из каталога» для селектов и `aria-label` «Добавить оружие из каталога» и т.д. для кнопок. **Осторожно:** Playwright ищет подстрокой, а Testing Library — точным совпадением, поэтому «Weapon from catalog» и «Add weapon from catalog» в Playwright всё равно конфликтуют — там нужен `{ exact: true }`.
- **Регрессия в чужом тесте от нового имени кнопки.** `getByRole("button", { name: /add weapon/i })` стал ловить и «Add weapon», и «Add weapon from catalog». Добавляя кнопку с именем-надмножеством, проверять существующие regex-запросы.

**Две сессии в одном рабочем дереве — отдельная опасность.** 2026-09-06 параллельно шли Evocations и снаряжение Core III. `git add -A` утащил чужие незакоммиченные файлы в мой коммит (откатил через `git reset --soft` + `git restore --staged`), а общие файлы — `src/types/character.ts`, оба локаля, фикстуры `makeCharacter` — правились обеими сессиями и по строкам уже не разделились: три пачки пришлось landed одним коммитом. **Если в дереве видна чужая незакоммиченная работа: коммитить только явными путями, а перед правкой общего файла перечитывать его с диска.**

**Временные скрипты проверки писать в scratchpad, а не в репозиторий.** Раньше `rm` дважды стоял после `&&` за вызовом Playwright, который падал, — файлы оставались в дереве, а `echo cleaned` создавал впечатление, что всё убрано. 2026-09-06 скрипты жили целиком вне репозитория и проблема не возникла вовсе; из репозитория Playwright при этом подключается по абсолютному пути (`import { chromium } from "file:///.../node_modules/playwright/index.mjs"`), иначе node его не находит. Если скрипт всё же оказался в репозитории — удалять отдельной командой и убеждаться через `ls check-*.mjs`.
